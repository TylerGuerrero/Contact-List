// route now will be /contacts because of index

const Contacts = () => {
    return (
        <div className="contacts">
            <h1>Contacts</h1>
        </div>
    );
}

export default Contacts;